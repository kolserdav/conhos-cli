services:
  node2:
    type: node
    version: 18.18.1
    size: mili
    commands:
      install: npm i
      build: npm run build
      start: npm run start
    ports:
      - 3000
    environment:
      PORT: '3000'
exclude:
  - dist
