project: hosting-cli
services:
  node1:
    type: node
    active: true
    image: 21-alpine3.18
    size: mili
    command: sh -c "echo Starting service... && npm install && npm run start"
    ports:
      - port: 3000
        type: http
    environment:
      PORT: 3000
exclude:
  - dist
