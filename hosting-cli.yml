project: hosting-cli
services:
  node0:
    type: node
    size: mili
    image: 21-alpine3.18
    command: sh -c "echo Starting service... && npm install && npm run start"
    ports:
      - 3000
    environment:
      - PORT=3000
exclude:
  - dist
